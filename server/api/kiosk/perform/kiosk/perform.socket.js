/**
 * Broadcast updates to client when the model changes
 */

'use strict';

// var KioskPerform = require('./kiosk/perform.model');

exports.register = function(socket) {
  // KioskPerform.schema.post('save', function (doc) {
  //   onSave(socket, doc);
  // });
  // KioskPerform.schema.post('remove', function (doc) {
  //   onRemove(socket, doc);
  // });
  // socket.emit('test')
}

// function onSave(socket, doc, cb) {
//   socket.emit('kiosk/perform:save', doc);
// }

// function onRemove(socket, doc, cb) {
//   socket.emit('kiosk/perform:remove', doc);
// }
